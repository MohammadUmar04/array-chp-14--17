//question no 21
// Declare and initialize an empty multidimensional array
const multiArray = [];

// Example: Adding empty arrays to create a 2D array
multiArray.push([]);
multiArray.push([]);
multiArray.push([]);

// Accessing and modifying elements in the 2D array
multiArray[0][0] = 1;
multiArray[0][1] = 2;
multiArray[1][0] = 3;
multiArray[1][1] = 4;

document.write(multiArray);
