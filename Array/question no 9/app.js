//question no 9

function newFunction() {
    var mov = ['avengers :age of ultron', 'spectre', 'jurasssic world', 'inside out'];
    for (var i = 0; i <= 3; i++) {
        document.write(i + ') ' + mov[i] + '<br>')
    }
}

  // Call the function to execute it
  newFunction();


  
