//question no 7

// Declare and initialize an array for mobile networks in Pakistan
var mobileNetworks = [
    'Mobilink',
    'Telenor',
    'Zong',
    'Ufone',
    'Warid', // Note: Warid was a mobile network in Pakistan; however, it has merged with Mobilink and is now part of Jazz.
    'Jazz'
  ];
  
  // Access and display the mobile networks
  for (var i = 0 ; i < mobileNetworks.length; i++) {
    document.write(mobileNetworks[i]);
  }
  